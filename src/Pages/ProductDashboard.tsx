import React from 'react';
import MenuAppBar from '../Components/AppBar';

const ProductDashboard: React.FC = () => {
    return (
        <>
            <MenuAppBar value={"Product List"} />
        </>
    );
};

export default ProductDashboard;
